export const PROJECT_CONFIG = {
  DATA_LOADING: {
    DO_NOT_LOAD: 0, // do not show loading indicator.
    FIRST_TIME_LOAD: 1, // load data for the first time. 
    EXTRA_ROUNDTRIP: 2 // Load extra dara using paging.
  },
  DEFAULT_PAGE_SIZE: 20,
};
